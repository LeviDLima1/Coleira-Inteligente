import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        gap: 15,
        marginTop: 15,
    },
    ball: {
        width: 70,
        height: 70,
        borderRadius: 100,
        backgroundColor: 'rgba(100, 43, 126, 0.2)',
    }
})
